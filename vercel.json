{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps && cd back-end && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "front-end/dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}